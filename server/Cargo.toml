[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
sailfish = "0.11.2"
tokio = { version = "1", features = ["full"] }
tokio-stream = { version = "0.1.18", features = ["full"] }
